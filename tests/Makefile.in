TARGET=test@EXEEXT@
CC=gcc
CFLAGS+=-Wall -g

all: $(TARGET)

run: $(TARGET)
	./$(TARGET)

$(TARGET): generate.c test.c CuTest.c
	$(CC) -o $@ $(LDFLAGS) $(CFLAGS) $(OBJ) $^

generate.c: test.c
	$(shell ./make-tests.sh test.c > generate.c)

clean:
	rm $(TARGET) generate.c
